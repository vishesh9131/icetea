# Backend API base (FastAPI). Required in production - Netlify only hosts the static UI.
# Leave empty for unified Netlify (same-origin /v1). Local dev defaults to :8000.
VITE_BACKEND_BASE=
# Unified Netlify build only (set by scripts/build-netlify-unified.sh)
# VITE_BASE=/app/
